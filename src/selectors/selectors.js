// import { createSelector } from '@reduxjs/toolkit';

// const cartItemsSelector = state => state.customers;

// export const cartTotalSelector = createSelector(
//     cartItemsSelector,
//     cartItems => cartItems.reduce((total, item) => total + (item.Gia), 0)
// );
